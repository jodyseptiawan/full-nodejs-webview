<div class="container mx-auto h-screen flex justify-center items-center">
  <form action="/todos" method="POST" class="w-[30%]">
    <h1 class="text-center text-2xl font-bold">Create Todo Form</h1>
    <div class="flex flex-col mt-8">
      <label for="title" class="text-sm">Title</label>
      <input
        id="title"
        name="title"
        type="text"
        placeholder="Type Title here ..."
        class="w-full border border-2 border-gray-400 p-2 rounded-md mt-1 focus:outline-none"
      />
    </div>
    <div class="mt-4 flex flex-col">
      <label for="note" class="text-sm">Note</label>
      <textarea
        id="note"
        name="note"
        placeholder="Type Note here ..."
        class="w-full border border-2 border-gray-400 p-2 rounded-md mt-1 focus:outline-none"
      ></textarea>
    </div>
    <div class="mt-10 flex flex-col">
      <button class="btn-create-todo rounded-md">Submit</button>
    </div>
  </form>
</div>